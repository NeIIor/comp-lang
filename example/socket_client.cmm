dim PORT;

main
{
    PORT = 9999;

    sock_init();

    s = sock_tcp_connect(127, 0, 0, 1, PORT);

    if (s < 0)
    {
        print(-1);
    }

    if (s >= 0)
    {
        sock_send_int(s, 42);

        print(sock_recv_int(s));

        sock_close(s);
    }
}
